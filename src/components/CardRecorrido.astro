---
const {
  parrafos = [],
  image,
  alt = "",
} = Astro.props;
---

<div
  class="md:inline-flex max-md:flex max-md:flex-col
         items-center gap-10
         border-2 border-white rounded-2xl
         p-6 max-md:p-4
         w-auto max-w-full"
>
  <div class="space-y-5 w-[350px] flex-none max-md:w-full max-md:p-2 max-md:text-sm">
    <slot />
  </div>

  <div class="w-[300px] flex-none max-md:w-full max-md:p-2">
    <img src={image} alt={alt} class="rounded-2xl w-full h-auto" />
  </div>
</div>
